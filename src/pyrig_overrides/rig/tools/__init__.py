"""Pyrig-specific overrides for external CLI tool wrappers.

Extends the tool definitions this package inherits from its dependencies
with behavior and configuration specific to publishing pyrig itself.
"""
