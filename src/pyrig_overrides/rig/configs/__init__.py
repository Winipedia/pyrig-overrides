"""Pyrig-specific overrides for managed configuration file definitions.

Extends the configuration definitions this package inherits from its
dependencies with metadata and settings specific to publishing pyrig itself.
"""
